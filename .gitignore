# secrets — never commit
.env
.env.local
.env.*.local

# dependencies & build output
node_modules
artifacts
cache

# runtime state — the gateway's append-only conformance history, generated per-deployment
services/conformance-log/

# generated documents (build from paper/*.md with pandoc)
*.docx
paper/full_paper_doc.md

# os
.DS_Store
Thumbs.db
